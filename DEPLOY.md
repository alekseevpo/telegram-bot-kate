# 🚀 Деплой на Railway - Пошаговая инструкция

## 📋 Предварительные требования

1. **GitHub аккаунт** - для хранения кода
2. **Railway аккаунт** - для хостинга
3. **Telegram Bot Token** - от @BotFather

## 🔧 Шаг 1: Подготовка GitHub репозитория

### 1.1 Создайте новый репозиторий на GitHub
- Перейдите на [github.com](https://github.com)
- Нажмите "New repository"
- Назовите репозиторий (например: `telegram-bot-kate`)
- Сделайте его **Public** (Railway требует публичный доступ)
- НЕ добавляйте README, .gitignore или лицензию

### 1.2 Инициализируйте Git в вашем проекте
```bash
# В папке bot_kate выполните:
git init
git add .
git commit -m "Initial commit: Telegram bot for Kate"
```

### 1.3 Подключите к GitHub
```bash
# Замените YOUR_USERNAME и REPO_NAME на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

## 🌐 Шаг 2: Настройка Railway

### 2.1 Создайте аккаунт на Railway
- Перейдите на [railway.app](https://railway.app)
- Войдите через GitHub

### 2.2 Создайте новый проект
- Нажмите "New Project"
- Выберите "Deploy from GitHub repo"
- Найдите и выберите ваш репозиторий

### 2.3 Настройте переменные окружения
В разделе "Variables" добавьте:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `BOT_TOKEN` | `ваш_токен_бота` | Токен от @BotFather |
| `ADMIN_ID` | `ваш_telegram_id` | Ваш ID (узнать у @userinfobot) |
| `ANONYMOUS_QUESTION_LINK` | `https://forms.google.com/...` | Ссылка на форму (опционально) |

### 2.4 Настройте домен (опционально)
- В разделе "Settings" → "Domains"
- Railway автоматически создаст домен вида: `your-project-name.up.railway.app`

## 🚀 Шаг 3: Деплой

### 3.1 Автоматический деплой
- Railway автоматически обнаружит Python проект
- Начнется сборка и деплой
- В логах вы увидите процесс установки зависимостей

### 3.2 Проверка логов
- В разделе "Deployments" нажмите на последний деплой
- Проверьте логи на наличие ошибок
- Успешный деплой покажет "Deploy Succeeded"

## ✅ Шаг 4: Проверка работы

### 4.1 Тест бота
- Откройте вашего бота в Telegram
- Отправьте команду `/start`
- Бот должен ответить приветствием

### 4.2 Проверка админки
- Отправьте команду `/admin`
- Если ваш `ADMIN_ID` настроен правильно, откроется админ-панель

## 🔄 Шаг 5: Обновления

### 5.1 Автоматические обновления
- При каждом push в GitHub, Railway автоматически передеплоит бота
- Просто обновите код и выполните:
```bash
git add .
git commit -m "Update: описание изменений"
git push
```

### 5.2 Ручной редеплой
- В Railway нажмите "Deploy" → "Deploy Now"

## 🚨 Решение проблем

### Проблема: "Build failed"
**Решение:** Проверьте логи сборки, убедитесь что все зависимости в `requirements.txt`

### Проблема: "Bot not responding"
**Решение:** 
1. Проверьте логи Railway
2. Убедитесь что `BOT_TOKEN` правильный
3. Проверьте что бот не заблокирован

### Проблема: "Module not found"
**Решение:** Убедитесь что все импорты в коде корректны

## 📊 Мониторинг

### Логи
- В Railway → ваш проект → "Deployments" → выберите деплой → "View Logs"

### Метрики
- Railway показывает использование ресурсов
- Бесплатный план: 500 часов/месяц

### Уведомления
- Railway уведомит о проблемах с деплоем
- Можно настроить уведомления в Discord/Slack

## 💰 Стоимость

- **Бесплатно:** 500 часов/месяц
- **Платно:** $5/месяц за 1000 часов
- **Масштабирование:** $0.000463 за дополнительный час

## 🎯 Преимущества Railway

✅ **Простота** - автодеплой из GitHub  
✅ **Надежность** - автоматические перезапуски  
✅ **Мониторинг** - встроенные логи и метрики  
✅ **Масштабируемость** - легко перейти на платный план  
✅ **SSL** - автоматический HTTPS  

---

**🎉 Поздравляю! Ваш бот теперь работает в облаке!**

*При возникновении проблем проверьте логи Railway и убедитесь что все переменные окружения настроены правильно.* 