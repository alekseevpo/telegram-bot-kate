# 🌐 Веб-интерфейс администратора Kate Bot

## 📝 Описание

Красивый веб-интерфейс для управления Telegram ботом Kate. Построен на Vue.js 3 + Vuetify с современным дизайном и удобной навигацией.

## ✨ Возможности

### 📊 Дашборд
- **Статистика в реальном времени**: пользователи, заказы, доходы
- **Быстрые действия**: переходы к разделам управления
- **Последние заказы**: мониторинг активности
- **Популярные продукты**: аналитика продаж

### 👥 Управление пользователями
- **Просмотр всех пользователей** с фильтрацией и поиском
- **Редактирование профилей**: имя, пол, этап, телефон
- **Детальная информация**: история заказов пользователя
- **Фильтры по этапам**: новые, активные, завершенные

### 📦 Управление продуктами
- **Создание новых продуктов** с названием, ценой и описанием
- **Редактирование существующих** продуктов
- **Удаление продуктов** с подтверждением
- **Просмотр каталога** с удобной таблицей

### 🛒 Управление заказами
- **Просмотр всех заказов** с информацией о заказчиках
- **Изменение статусов** заказов (ожидание, подтвержден, завершен, отменен)
- **Детали заказа**: состав, суммы, даты
- **Фильтрация по статусам**

### 🔔 Уведомления и рассылки
- **Создание массовых уведомлений** для пользователей
- **Выбор целевой аудитории**: все, новые, активные, завершенные
- **Планирование отправки** на определенное время
- **История уведомлений** с отслеживанием статусов

## 🏗️ Архитектура

```
web-admin/
├── 📄 index.html              # Главная HTML страница
├── 📦 package.json            # Зависимости проекта
├── ⚙️ vite.config.js          # Конфигурация Vite
└── src/
    ├── 🚀 main.js             # Точка входа приложения
    ├── 📱 App.vue             # Главный компонент
    ├── 🛣️ router.js           # Маршрутизация
    ├── 🌐 services/
    │   └── api.js             # API сервисы
    └── 📄 views/
        ├── Login.vue          # Страница авторизации
        ├── Dashboard.vue      # Дашборд
        ├── Users.vue          # Управление пользователями
        ├── Products.vue       # Управление продуктами
        ├── Orders.vue         # Управление заказами
        └── Notifications.vue  # Уведомления
```

## 🚀 Установка и запуск

### 1. Требования
- **Node.js 18+** и npm
- **Python 3.8+** с установленными зависимостями бота
- **Запущенный API сервер** (`api.py`)

### 2. Установка зависимостей

```bash
# Перейти в папку веб-интерфейса
cd web-admin

# Установить зависимости
npm install
```

### 3. Запуск в режиме разработки

```bash
# Запустить веб-интерфейс (порт 3000)
npm run dev
```

### 4. Запуск API сервера

```bash
# В отдельном терминале, в корневой папке проекта
cd ..
python api.py
```

API будет доступен на `http://localhost:8000`

### 5. Доступ к интерфейсу

Откройте браузер и перейдите по адресу:
```
http://localhost:3000
```

**Для входа используйте ваш ADMIN_ID из файла `.env`**

## 🔧 Конфигурация

### API соединение
Веб-интерфейс автоматически проксирует запросы к API через Vite:
- Фронтенд: `http://localhost:3000`
- API: `http://localhost:8000`

### Авторизация
Простая авторизация по Admin ID:
- Токен: `admin_{ADMIN_ID}`
- Хранится в localStorage браузера

## 🎨 Дизайн

### Цветовая схема
- **Основной**: #1976D2 (синий)
- **Успех**: #4CAF50 (зеленый)
- **Предупреждение**: #FB8C00 (оранжевый)
- **Ошибка**: #FF5252 (красный)

### Компоненты UI
- **Vuetify 3**: современные Material Design компоненты
- **Material Design Icons**: красивые иконки
- **Адаптивный дизайн**: работает на всех устройствах

## 📱 Мобильная версия

Веб-интерфейс полностью адаптивен:
- **Responsive дизайн** для телефонов и планшетов
- **Мобильная навигация** с боковым меню
- **Touch-friendly** интерфейс
- **Быстрая загрузка** и производительность

## 🔄 API эндпоинты

### Дашборд
- `GET /api/dashboard/stats` - статистика

### Пользователи
- `GET /api/users` - список пользователей
- `GET /api/users/{id}` - детали пользователя
- `PUT /api/users/{id}` - обновление пользователя

### Продукты
- `GET /api/products` - список продуктов
- `POST /api/products` - создание продукта
- `PUT /api/products/{id}` - обновление продукта
- `DELETE /api/products/{id}` - удаление продукта

### Заказы
- `GET /api/orders` - список заказов
- `PUT /api/orders/{id}/status` - изменение статуса

### Уведомления
- `GET /api/notifications` - история уведомлений
- `POST /api/notifications` - создание уведомления

## 🚀 Производственная сборка

```bash
# Создать production сборку
npm run build

# Предварительный просмотр
npm run preview
```

Сборка будет создана в папке `dist/`

## 🎯 Следующие шаги

1. **Развертывание**: деплой на Vercel, Netlify или VPS
2. **Безопасность**: улучшенная аутентификация (JWT)
3. **Аналитика**: графики и диаграммы продаж
4. **Push-уведомления**: реальное время обновления
5. **Экспорт данных**: CSV/Excel отчеты

## 🎉 Готово!

Теперь у вас есть современный веб-интерфейс для управления ботом! 🎊

**Возможности:**
- ✅ Красивый дашборд с аналитикой
- ✅ Управление пользователями и продуктами
- ✅ Обработка заказов и уведомлений
- ✅ Мобильная адаптация
- ✅ Современный дизайн

---

*Создано с ❤️ для удобного управления Kate Bot* 